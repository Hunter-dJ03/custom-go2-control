lidar_processor:
  ros__parameters:
    min_range: 0.1
    max_range: 30.0
    min_height: -0.3
    max_height: 2.0
    publish_2d_scan: true
    scan_height: 0.0

camera_processor:
  ros__parameters:
    calibration_file: ""
    undistort: false

sensor_tf:
  ros__parameters:
    lidar_frame: "lidar_link"
    front_camera_frame: "front_camera_link"
    imu_frame: "imu_link"
